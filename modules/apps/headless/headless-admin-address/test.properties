##
## Test Batch
##

    #
    # Portal Hotfix Release
    #

    test.batch.run.property.query[functional-bundle-tomcat-mysql57-jdk8][test-portal-hotfix-release]=\
        ${test.batch.run.property.query[functional-tomcat*-mysql*-jdk8][relevant][headless-admin-address-rule]}

    #
    # Relevant
    #

    test.batch.run.property.query[functional-tomcat*-mysql*-jdk8][relevant][headless-admin-address-rule]=\
        (portal.acceptance == true) AND \
        (testray.main.component.name ~ "Address")

##
## Test Suites
##

    test.batch.names[relevant][headless-admin-address-rule]=\
        functional-bundle-tomcat-mysql57-jdk8,\
        functional-tomcat*-mysql*-jdk8,\
        functional-tomcat*-mysql*-jdk8

    relevant.rule.names=\
        headless-admin-address-rule

    modified.files.includes[relevant][headless-admin-address-rule]=*

##
## Testray
##

testray.main.component.name=Address